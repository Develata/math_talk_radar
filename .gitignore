# Rust build artifacts
/target
**/*.rs.bk

# Editor / OS noise
.DS_Store
*.swp
*.swo

# Local codegraph index (per-machine, not project truth)
/.codegraph
/.codegraph/

# Orchestration / agent scratch (per-machine, not project truth)
/.omo
/.omo/

# Coverage / xtask artifacts
*.profraw
/coverage
