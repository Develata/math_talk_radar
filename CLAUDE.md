# CLAUDE.md

This repository uses `AGENTS.md` as the canonical agent instruction file.

Read `AGENTS.md` and the nearest nested `AGENTS.md` (under `crates/*`, `apps/cli`,
or `docs/`) before modifying files. The engineering constitution and plan files
under `docs/plan/` are the design truth source; code is a projection of the plan.
